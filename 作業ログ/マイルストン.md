# vibe-cast マイルストン整理

*最終更新: 2026-04-07*

---

## 全体方針

```
master（本番）
└── feature/ai-trend-dashboard（Epic ブランチ）
    └── 各 Issue ブランチ → Epic へ PR → Epic 完成後に master へ PR
```

---

## Milestone 0: 前提修正（完了）

| Issue | PR | 内容 | 状態 |
|-------|----|------|------|
| #16 | #24 | タブ切替時に録音が継続されるよう display:none 方式に変更 | ✅ master マージ済み |

---

## Milestone 1: データパイプライン構築（バックエンド）

Epic: #17「AI トレンドダッシュボード」のバックエンド部分。
`backend/app/trend_pipeline/` 以下に実装。

### Issue #18: arXiv 取得 + Gemini キーワード抽出 + 共起 JSON 生成
**ブランチ:** `feature/issue-18-arxiv-pipeline`
**PR:** #25（`feature/ai-trend-dashboard` へ）
**状態:** ✅ PoC 完成・レビュー待ち

やること:
- [x] arXiv API から cs.AI カテゴリ直近1週間の論文を取得
- [x] Gemini 2.5 Flash でキーワード抽出
- [x] 正規化（複数形統一）+ 共起計算 → nodes/edges JSON 出力
- [ ] 論文数を 30 → 100件に増やして密なネットワークを確認
- [ ] FastAPI エンドポイント化（`GET /trend/network`）

---

### Issue #19: YouTube Data API でキーワード関連動画メタデータ取得
**ブランチ:** 未作成
**状態:** 未着手（#22 サイドパネルと一緒にやる）

やること:
- [ ] GCP で YouTube Data API v3 を有効化・APIキー取得
- [ ] キーワードをクエリとして動画上位3〜5件のメタデータ取得
- [ ] ノード JSON に動画データを紐付けて保存

---

### Issue #20: LLM による論文の3行要約 + ビジネス示唆生成
**ブランチ:** 未作成
**状態:** 未着手（#22 サイドパネルと一緒にやる）

やること:
- [ ] Gemini プロンプト調整（①背景・②手法・③結果 + ビジネス示唆）
- [ ] 論文ごとの要約を Firestore or GCS に保存

---

## Milestone 2: ダッシュボード UI 実装（フロントエンド）

### Issue #21: React Flow で共起ネットワーク UI（トレンドタブ新設）
**ブランチ:** `feature/issue-21-network-ui`
**PR:** #26（`feature/ai-trend-dashboard` へ）
**状態:** ✅ MVP 完成・レビュー待ち

やること:
- [x] App.tsx に trend タブ追加（display:none 方式）
- [x] TrendDashboard.tsx 新設・React Flow で描画（ハードコードデータ）
- [ ] ハードコードデータ → `GET /trend/network` API 取得に差し替え（#18 完了後）

---

### Issue #22: ノードクリック → 詳細サイドパネル
**ブランチ:** 未作成
**状態:** 未着手（#19・#20 完了後に着手）

やること:
- [ ] ノードクリック → 右からスライドインする Drawer を実装
- [ ] 選択キーワードの論文リスト + 3行要約を表示
- [ ] YouTube iFrame 埋め込みプレーヤーを配置

---

## Milestone 3: 自動化とデプロイ

### Issue #23: Cloud Run + Cloud Scheduler で週次バッチ自動化
**ブランチ:** 未作成
**状態:** 未着手

やること:
- [ ] パイプラインスクリプトを Cloud Run Job としてデプロイ
- [ ] Cloud Scheduler で毎週月曜朝にトリガー
- [ ] フロントエンド（トレンドタブ含む）を Firebase Hosting にデプロイ

---

## MVPへの最短ルート

```
✅ #16  タブ切替バグ修正（マージ済み）
✅ #18  データパイプライン PoC（PR #25 レビュー待ち）
✅ #21  React Flow UI MVP（PR #26 レビュー待ち）
  ↓
  #18  論文数を増やして密なネットワーク確認
  #18  FastAPI エンドポイント化
  #21  API 取得に差し替え（絵が本物データで出る）
  ↓
  #22  ノードクリック → 論文一覧表示（サイドパネル）
  #19  YouTube 動画レコメンド（サイドパネルの中身）
  #20  論文3行要約（サイドパネルの中身）
  ↓
  #23  本番デプロイ・週次自動更新
```

YouTube（#19）と3行要約（#20）はサイドパネル（#22）の中身なので、
#22 と同時並行で実装するのが効率的。

---

## 既存機能の未着手 Issue

| Issue | 内容 | 優先度 |
|-------|------|--------|
| #6 | TURN サーバー追加（企業ネット等での接続改善） | 中 |
| #10 | BGM 自動挿入 | 低 |
| #13 | note 自動投稿 | 低 |
| #14 | RSS 生成・ポッドキャスト配信 | 低 |
